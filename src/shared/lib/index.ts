// App-wide constants and other shared library code go here.
// The Supabase client lives in @/shared/lib/supabase — import it directly
// from there (see CLAUDE.md's "How to add a new feature"), not from this barrel.
export {}
